# v0.8.2 Mainline Alignment Evidence

Date: 2026-04-27
Evidence level: repository hygiene + internal deterministic verification. This is not external validation.

## Purpose

v0.8.0 and v0.8.1 were published on the v0.8 feature line, but `master` had remained behind on a v0.7-era tree. v0.8.2 aligns `master` with the current v0.8 repository structure and closes the value-driven enhancer work.

## Mainline Alignment

Before alignment:

- `master` pointed at `5102587 Document v0.7.1 audit response and v0.7.2 design`.
- The v0.8.1 repository structure lived on `feature/v0.7.3-evaluation-taxonomy`.
- The current development worktree path was still named `.worktrees/v072-live-fact`, which was semantically stale.

Action taken:

- `master` was fast-forwarded to `71c30a0 Add value-gain pressure enhancer`.
- v0.8.2 bumps package version to `0.8.2`.
- v0.8.0 and v0.8.1 are left untouched as historical releases.

## Structure Baseline

The aligned master tree now uses the v0.8 repository layout:

- `docs/` for public, methodology, engineering, release-history, and archive docs.
- `evidence/` for current and historical evidence.
- `review-pack/current/` for the current reviewer-facing artifact pack.
- `_archive/` for archived imports such as old Notion exports.
- no tracked top-level `reports/`, `notion-export/`, or `workflow_candidates/` directories.

## Value-Driven Enhancer Closure

The current default generated SKILL chain includes:

- `value_gain_decision`
- `value_gain_evidence`
- `value_gain_risk_boundary`
- `value_gain_next_handoff`
- `Downstream Use Check`
- `Minimum Pressure Pass`

Supporting evidence:

- `evidence/current/2026-04-26-value-gain-generation-chain-evidence.md`
- `evidence/current/2026-04-26-user-perspective-value-gain-ab-review.md`
- `evidence/current/2026-04-26-value-gain-c-pressure-chain-evidence.md`

## Remaining Cleanup Risk

Old local worktrees still exist under `.worktrees/`. At least `v0.6-w2` has dirty local changes and should not be deleted without separate review. This does not affect the v0.8.2 tree, but it remains local repository hygiene debt.

## Verification

Release verification run from the aligned `master` tree:

- Focused fixture regression: `PYTHONPATH=src python3 -m unittest tests.test_pipeline.CandidatePipelineTests.test_example_fixture_candidates_use_seeded_quality_content tests.test_pipeline.CandidatePipelineTests.test_preflight_rejects_generated_skill_summary_drift tests.test_pipeline.CandidatePipelineTests.test_scaffolded_example_bundle_prefers_inherits_from -v` -> 3 tests OK.
- Full unit regression: `PYTHONPATH=src python3 -m unittest discover tests -v` -> 281 tests OK.
- Build: `python3 -m build --no-isolation` -> built `kiu-0.8.2.tar.gz` and `kiu-0.8.2-py3-none-any.whl`.
- Install smoke: `/tmp/kiu-v082-install-smoke/bin/python -m pip install --force-reinstall dist/kiu-0.8.2-py3-none-any.whl` followed by imports for `kiu_pipeline.blind_review_pack`, `kiu_pipeline.book_pipeline`, `kiu_pipeline.draft`, and `kiu_validator.core` -> OK.

The first install smoke intentionally tried `--no-deps` and failed on missing `PyYAML`; the normal dependency-resolving install path succeeded after network access was available.
