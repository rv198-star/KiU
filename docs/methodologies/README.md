# 方法论工具箱

这组文档用于保存 KiU 会调用的方法论工具。`AGENTS.md` 只定义项目调用协议和边界；方法论本体尽量保持通用、可复用，不写成 KiU 专属制度。

## 项目硬边界

项目硬边界保留在 `AGENTS.md`，不属于方法论工具箱。所有方法论调用都必须遵守这些边界，包括来源忠实、证据诚实、外部参考边界、流程/判断边界和应用校准隔离。

## KiU 原生方法论

用于解释 KiU / 学以致用 自身如何工作。

| 方法论 | 作用 | 详细文档 |
| --- | --- | --- |
| 学以致用方法论 | 把原书知识转化为可执行行动力。 | [学以致用方法论](kiu-methodology.md) |

## 通用思考工具

这些方法可以用于 KiU，也可以用于其他项目。它们不是 KiU 独有方法论；在 KiU 内部使用时，必须遵守 KiU 项目硬边界。

| 方法/原则 | 作用 | 详细文档 |
| --- | --- | --- |
| 系统效率碾压局部优势 | 判断长期主战场是否应押注系统级费效比更高的路线。 | [顶层决策哲学](top-level-decision-philosophy.md) |
| 三层递归五步法 | 发现问题、定义问题、解决问题，每层都按基线、目标、差距、策略、拆解推进。 | [三层递归五步法](recursive-five-step-method.md) |
| 极限演绎与场景投影法 | 面对 A/B 都有道理的模糊问题，先做结构诊断，再做场景选型。 | [极限演绎与场景投影法](extreme-deduction-and-scenario-projection.md) |

## 外部工具方法论

这些方法来自外部材料或独立工具包。KiU 可以在开发、审计、实验中调用它们，但它们不是 KiU 原生方法论，也不进入公开产品主叙事。调用时必须标明边界，不能把工具方法论混入 source-derived SKILL 正文。

| 方法/原则 | 作用 | 详细文档 |
| --- | --- | --- |
| 模块价值增益法 | 当一个模块已经结构完整但价值偏薄时，做有限轮价值增益审计，判断是否需要加深、返工或冻结。 | [模块价值增益法](external-thinking-value-gain-methodology.md) |

## 调用协议

产品和公开叙事优先使用学以致用方法论，避免用历史外部术语解释项目自身。

复杂问题推进优先使用三层递归五步法。

如果问题呈现为“A 也行、B 也行”的结构模糊，再调用极限演绎与场景投影法。

如果问题涉及长期路线、主战场选择或系统方案 vs 局部卓越，再调用系统效率碾压局部优势做一次宏观校准。

如果某个具体模块已经成形，但仍可能只是“结构完成、价值偏薄”，再调用外部模块价值增益法做模块级审计；该方法只服务具体模块，不参与项目方向决策。

模块价值增益法当前只有 Financial Statement 单书 A/B 初测证据。五本齐发验证完成前，不得把它写成跨书稳定增益结论。

## 边界

- 通用思考工具是外挂工具，不是 KiU 产品身份。
- 外部工具方法论是更外层的可选工具，不是 KiU 原生方法论。
- 系统效率原则是提醒式原则，不是机械结论。
- 任何方法论都不能覆盖证据诚实、流程/判断边界、外部参考边界和应用校准隔离。
- 内部方法论可以指导开发，但不能自动升级为真实用户验证或外部评审证据。
